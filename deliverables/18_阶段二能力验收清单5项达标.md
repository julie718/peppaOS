# 阶段二·能力验收清单（5 项，全部达标）

- 阶段：阶段二（自诊疗 self-healing 模块）
- 日期：2026-08-08
- 验收方式：E2E 自动化验证（S8 五场景 23 条断言）+ 隔离库接口实测 + git diff 审核
- 结论：**5/5 全部达标（100%）**

---

## 验收项 1：后台静默全域定时自检引擎 ✅

| 要求 | 证据 |
|------|------|
| scheduler 低频任务 | `self_heal_daily` cron `0 3 * * *`（每日 3:00，quiet）已注册：`[Scheduler] Registered cron task "self_heal_daily"` |
| 手动触发接口 | POST /api/system/health-check 实测返回 runId `SH-2026-08-08T05-45-57` |
| 73 条原始标准 E2E 断言离线运行 | assertions.ts 恰好 73 条（SH-A001~A073），实测 73/73 通过 |
| 隔离 DB/目录 | SELF_HEAL_DB_PATH 独立库 + E2E rootDir 注入 /tmp/lumi_selfheal_e2e（S8-L4d 验证） |
| 结构化缺陷报告 | 编号/文件+行号/症状/判定标准/模板匹配标识（S8-L4b 验证） |
| 持久化历史 | self_heal_records + self_heal_defects 双表，GET historyCount=5（S8-L4c 验证） |

## 验收项 2：缺陷自动溯源定位 ✅

| 要求 | 证据 |
|------|------|
| 4 类故障：断言失败/运行时异常/死代码/硬编码常量 | detector.ts 四扫描器全部实现 |
| 全局 grep 调用链 | scanReferences 全仓引用扫描（含根级 server.ts） |
| 已知高频 bug 与未知缺陷分类 | classifyKnownDefects：S8-L1d 命中 TPL-L1（known/autoRepairable）、S8-L2c 命中 TPL-TTL |
| 分级告警 | P1/P2/P3 三级：S8-L1d severity=P1、遗留硬编码 P2、死代码 P3 |

## 验收项 3：标准化修复模板库 + TS 轻量源码编辑引擎 ✅

| 要求 | 证据 |
|------|------|
| E/L/O 高频 bug 模板 | templates.ts 9 模板（情绪收敛/人格冷却/TTL 链路/GC 扫描/模型硬编码/confidence 兜底等） |
| 只读校验 + 安全修改 | editor.ts 修改前快照（.self_heal_snapshots/） |
| 修改前快照备份 | S8-L5b 验证快照 ≥2 份 |
| 单次修复后 tsc 语法检查 | checkSyntax（transpileModule）：S8-L3a 坏修改被拦截 |
| 语法错误自动回滚 | S8-L3b/c 回滚成功 + 文件恢复原内容 |

## 验收项 4：修复后回归 + 自动回滚 ✅

| 要求 | 证据 |
|------|------|
| 局部 E2E 子集 / 全量 | regression.ts runLocalRegression（按 file 关联断言）/ runFullRegression |
| 失败一键回滚（快照） | S8-L3c 快照回滚生效 |
| 回滚日志 | rollback.log 留痕（S8-L3d 含缺陷编号 D9999） |
| 热重载 | 引擎每次自检重读磁盘文件，修复即时生效（S8-L1f 修复落盘验证） |

## 验收项 5：/api/system/health-check 查询接口 ✅

| 要求 | 证据 |
|------|------|
| 最新报告 | GET 返回 latestRunId |
| 开放缺陷数 | openDefects=105 |
| 待自动修复条目 | pendingAutoRepairs=0 |
| 历史修复记录 | repairHistoryCount（S8-L4f 含 TPL-L1 自动修复） |
| 健康评分 | healthScore=60 + verdict=degraded（模型：100 − P1:20/P2:10/P3:2 封顶 20） |

---

## E2E 新增 5 个 S8 场景（指令要求逐条对照）

| 指令场景 | E2E 断言 | 状态 |
|----------|----------|------|
| L-1 模拟修复（检测→修复→回归闭环） | S8-L1a~g（7 条） | ✅ |
| TTL 死代码捕获+修复 | S8-L2a~e（5 条） | ✅ |
| 错误修改回滚 | S8-L3a~d（4 条） | ✅ |
| 手动触发报告输出 | S8-L4a~f（6 条） | ✅ |
| 隔离不影响正式数据 | S8-L5a~c（3 条） | ✅ |

**E2E 全量结果：149 通过 / 0 失败（TZ=America/New_York）**

# 变更明细 — 非必要固化模板清理重构

> 统计：51 文件，+893 / -4,441 行（git diff 基线 d3d9d70）
> 新增文件：`server/proactive/rhythm.ts`（M2 统计学习底座，未跟踪→新增）

---

## 一、删除文件（9 个，2,220 行）

| 文件 | 行数 | 内容 | 归属 |
|------|------|------|------|
| server/agents/auto_installer.ts | 187 | 死代码（无调用者） | 模块1 附带 |
| server/agents/nl_chainer.ts | 420 | 自然语言指令链静态分支（死代码） | 模块1 |
| server/cognition/quick_commands.ts | 322 | 指令正则池 + 静态工具映射（死代码） | 模块1/5 |
| server/cognition/tool_intent.ts | 109 | 关键词→工具意图静态映射 | 模块5 |
| server/cognition/tool_router.ts | 432 | 静态工具路由表（directToolCall 后端） | 模块5 |
| server/cognition/work_surface.ts | 95 | 死代码 | 模块1 |
| server/life/comprehension.ts | 112 | 理解度正则池 + 固定追问模板（零调用者） | 模块4 |
| server/tools/responseFormatter.ts | 310 | RAIN_TIPS/SUN_TIPS 等 8 个固定话术格式化器 | 模块4 |
| test/tool_router.test.ts | 233 | 随 tool_router 删除的测试 | 模块5 |

## 二、删除文件引用清理

- `life/index.ts`：comprehension import、TICK 步骤 1.6、re-export（4 处）
- `hooks/chat.ts`：comprehensionOverall 参数 + 「整体理解度」模板行
- `socket/chat.ts`：comprehension import + buildMindContext 实参
- `personality/index.ts`：generateContextualGreeting 导出

## 三、修改文件（按模块）

### 模块1 意图识别层（→ 心智 LLM）
| 文件 | 改动 |
|------|------|
| server/cognition/intent.ts | 删除 9 组正则池（GREETINGS/SMALL_TALK/COMMAND_PATTERNS/QUESTION/CODE/WEB/FILE/SYSTEM/AGENT）+ POSITIVE/NEGATIVE 情绪关键词表 + directToolCall；保留 LLM 分类器（classifyIntentLLM，LRU 缓存）+ 类型 + 情绪透传；CLASSIFIER_PROMPT 新增 correction 实体提取规则；**新增 correction 实体归一化**（布尔/字符串 → 'true'） |
| server/cognition/index.ts | processInput 统一心智分类入口（无正则前置）；directToolExecuted 恒 false 注释化 |
| server/cognition/selfState.ts | LIFE_DB 路径统一 + 构造器空回调（见 §4） |
| server/cognition/vision_routing.ts | 相关引用清理 |
| server/socket/chat.ts | correctionPatterns 正则池 → `cognition.intent?.entities?.correction === 'true'` 门控 |
| server/routes/chat_routes.ts | SELF_AWARE_PATTERNS 正则池 + getVitality 导入删除（REST 路径遗漏，WS 早已移除） |
| test/intent.test.ts | 断言语义更新为心智新行为（编号保留） |

### 模块2 主动行为触发引擎（→ 统计学习）
| 文件 | 改动 |
|------|------|
| server/proactive/rhythm.ts（新增） | PRIOR 锚点常量（保留类别⑥，值与原硬编码一致保 E2E 数字）+ deriveThresholds（gapFactor/careSensitivity/行程窗口/记忆跟进）+ deriveQuietWindow/deriveMorningWindow（活跃直方图学习，无数据回落 23-7/6-10）+ composeTriggerContent（LLM 润色 ≤80 字 + 离线回退结构化摘要）；**require → 静态 import**（修复润色分支从未生效） |
| server/proactive/triggers.ts | 198 行重写：所有内联阈值（23-7/6-10/6h·12h·48h/24h·18h·8h·3d）→ deriveThresholds/deriveQuietWindow/deriveMorningWindow/isQuietNow |
| server/life/userState.ts | isLateNight 硬编码 23-7 → rhythm 派生静默窗口（懒加载 require 避环，容灾回落先验） |
| server/heartbeat/injector.ts | generateLowEnergyMessage 调用加 await |

### 模块3 金融标的市场识别（验证，无改动）
- 确认无静态标的清单；normalizeStockCode/detectMarket 数据层保留（场景3 验证通过）

### 模块4 对话回复生成层（→ 心智润色）
| 文件 | 改动 |
|------|------|
| server/services/weather.ts | getTimeGreeting（morning/afternoon/evening/night 固定问候数组）删除；getWeatherBrief 返回原始天气文本 |
| server/scheduler.ts | 晨间/晚间摘要模板句（「N 条待办/记得你最近聊过」）→ composeTriggerContent('morning_digest'/'evening_wrapup') |
| server/personality/state.ts | generateContextualGreeting（亲密分区×时段×离开时长 72/24/8/<1 分支 + 节假日后缀）删除 |
| server/socket/chat.ts | 重逢问候块 → composeTriggerContent('reconnect_greeting', {intimacy, hoursAway})（hoursAway<1 不发） |
| server/autonomy/idle_brain.ts | MONOLOGUE_TEMPLATES（8 情绪×2-3 条独白池）删除 → composeTriggerContent('inner_monologue') |
| server/autonomy/psi_motivation.ts | 「📰 空闲资讯简报（最近 72h N 条）」固定表头删除 → 纯数据序列 |
| server/life/vitality.ts | generateSelfAwareResponse 死代码整删；generateLowEnergyMessage 固定话术池 → composeTriggerContent('vitality_low_energy') |
| server/socket/voice.ts | 6/12/18 时段固定问候回退 → composeTriggerContent('voice_return_greeting')（主路径 LLM 生成保持） |
| server/tools/definitions/weather_tools.ts | 格式化器调用 → JSON.stringify 结构化返回（8 处） |
| server/tools/definitions/news_tools.ts | CATEGORY_KEYWORDS 死代码删除；格式化器 → JSON.stringify |
| server/tools/definitions/calendar_tools.ts | 格式化器 → JSON.stringify（4 处） |
| server/tools/mcp_servers/web_search.ts | MUST_SEARCH_TERMS 文档化为统计先验锚点；fetchMultiSource 拆 fetchAllRaw/dedupeItems（行为不变）；websearchMulti 泛化：GENERIC_TOPIC_REQUESTS → deriveSearchTerms 多词匹配（user_preferences 资讯标签数据驱动） |
| server/music/library_profile.ts / search_play.ts | 相关固定表述清理 |
| server/agents/background_delegation.ts / orchestrator.ts | 固定话术/模板清理 |
| server/socket/task.ts | 相关模板清理 |
| server/runtime/routes.ts | 路由挂载补充（+4） |
| test/background_delegation.test.ts / vision_routing.test.ts | 断言语义随新行为更新 |

### 构建与测试
| 文件 | 改动 |
|------|------|
| scripts/build-server.mjs | 删除 proactive/ 复制+tsc 编译块（恒失败 TS2307 被 catch 吞、误报 "not found, skipping"；esbuild 已静态打包 proactive 无外部化 require） |
| e2e_isolated_25fix.test.ts | 断言总数保持 149（S1-S7 124 + S8 25），验证目标更新为心智新行为：S5-9/10 深夜 23 点不触发（D-1）、S7-A8 行程推送（D-2）、S8 自诊疗断言等 |

## 四、SQLITE_CANTOPEN 启动崩溃修复（校验暴露，6 处）

| 文件 | 修复 |
|------|------|
| server/config/data_path.ts | getPeppaDbPath 默认 `/app/data/peppa.db` → `getDataPath('peppa.db')`（无 DB_PATH 时回落数据根；Docker 经 LUMI_DATA_DIR=/app 行为完全一致；DB_PATH 环境变量优先保留） |
| server/life/index.ts:144 | `new sqlite3.Database(peppaDbPath)` → 加空回调（防未处理 'error' 事件型崩溃） |
| server/life/relationshipAwareness.ts:457 | 同加空回调 |
| server/cognition/selfState.ts | LIFE_DB 默认 → `getDataPath('life.db')` + 构造器加回调 |
| server/life/direction.ts | LIFE_DB 同改 + getDb() 加回调 |
| server/db/lifeDb.ts | DB_PATH 默认 → `getDataPath('life.db')`（原有回调保留） |

## 五、保留但文档化（防误删）

- rhythm.ts PRIOR 常量（保留类别⑥，无数据锚点）
- web_search.ts MUST_SEARCH_TERMS（E2E S7-B4 锚点）
- stock_fin.ts 合规（场景3 验证）
- self_heal/ 目录（阶段二契约，未跟踪属正常）

# 阶段一 全量代码变更总表

> 交付物 ① —— Claude Code 阶段一一次性完工（5 套标准 MCP + DAG 调度 + 数字孪生 + PSI 动机引擎 + 多路径推理 + 情绪记忆闭环）
> 校验基线：tsc 0 错误 / build-server 0 失败 / 全新隔离库启动 health 200 / E2E **121/121 全绿** / 四维 33/33 / git diff 无历史回退

## 一、新增文件（11 个）

| 文件 | 行数 | 模块 | 职责 |
|------|------|------|------|
| `server/tools/mcp_servers/travel_cal.ts` | 288 | 1 | travel-cal-mcp：AES-256-GCM 加密行程 CRUD + 提醒阈值 + 天气/POI/航班/酒店 + 偏好过滤 + `pushUpcomingTravelInfo` 行程临近推送 |
| `server/tools/mcp_servers/web_search.ts` | 167 | 1 | web-search-mcp：8 源并发抓取 + 24h/7d 时效过滤 + 标题去重多源标注 + 多源对比去偏见 + `MUST_SEARCH_TERMS` 强制检索词 |
| `server/tools/mcp_servers/stock_fin.ts` | 151 | 1 | stock-fin-mcp：腾讯行情/K线 + 东方财富公告/板块（免费接口），仅客观陈列，全部带免责声明 |
| `server/tools/mcp_servers/notify.ts` | 69 | 1 | notify-mcp：通知表 + socket 主动推送 + 终端日志三通道推送 |
| `server/tools/mcp_servers/util.ts` | 221 | 1 | util-mcp：BigInt 定点 18 位高精度计算（0.1+0.2=0.3 无误差）+ 单位换算 + 货币汇率 + 多文档解析统一入口 |
| `server/tools/mcp_servers/mcp_helpers.ts` | 47 | 1 | 标准 MCP 工厂共享构建器（zod schema + capabilities 第二参数），5 套 MCP 零重复编码 |
| `server/tools/mcp_servers/index.ts` | 19 | 1 | 5 套 MCP 汇总注册 + McpServer 工厂导出 |
| `server/autonomy/digital_twin.ts` | 116 | 2 | 数字孪生行为模型：五维标签采集（出行/阅读/理财/情绪/作息）+ 行为预判（纯函数可测） |
| `server/autonomy/psi_motivation.ts` | 130 | 2 | PSI 内在动机引擎：好奇心/陪伴/规划三需求张力 + 新老用户推送频次区分 + 空闲资讯简报 |
| `server/cognition/multi_path_reasoner.ts` | 172 | 3 | 并行多路径因果推理：理性实证/反面证伪/多因合流三链路并行 + 交叉校验（共识/分歧判定）+ `tryMultiPathFallback` LLM 失败兜底 |
| `server/tools/definitions/cognition_tools.ts` | 52 | 3 | `cognitive_reason` 工具注册（LLM 深度推理场景主动调用） |

## 二、修改文件（10 个，全部增量接线，无历史逻辑回退）

| 文件 | 改动点 | 模块 |
|------|--------|------|
| `server/db/lifeDb.ts` | +`travel_itineraries` 表（encrypted TEXT 存密文）+ 6 个 CRUD 导出函数（add/list/get/update/delete/getUpcomingTravels） | 1 |
| `server/tools/definitions/weather_tools.ts` | 导出 `cityCoords`/`resolveCoords`/`fetchWeather`/`fetchWeatherByCity`（复用，不重复实现） | 1 |
| `server/tools/definitions/news_tools.ts` | 导出 `NEWS_SOURCES`；`fetchFromSources` 增加可选第 3 参 `fetchRSSImpl`（复用，不重复实现） | 1 |
| `server/tools/definitions/index.ts` | `registerAllTools` 追加 `registerMcpServers(registry)` + `registerCognitionTools(registry)`（5 套 MCP + 深度认知全量接入工具注册表） | 1/3 |
| `server/proactive/triggers.ts` | 新增 `travelUpcomingTrigger`（72h 临近行程批量拉取推送，8h 冷却，无活跃用户跳过）加入 `allTriggers`（6→7 个） | 2 |
| `server/proactive/index.ts` | `ProactiveScene` 联合类型新增 `'travel_upcoming'` | 2 |
| `server/autonomy/idle_brain.ts` | 长待机步骤 4 后新增步骤 5：空闲资讯简报（`generateIdleBriefing`，复用 NEWS_SOURCES 多源底座） | 2 |
| `server/life/personality.ts` | `adaptToEvent` 事件类型新增 `'news_reading'`：好奇心+0.003 / 开放性+0.003（资讯阅读后缓慢微调 8 维人格） | 3 |
| `server/socket/chat.ts` | 两处 LLM 全失败兜底前新增多路径交叉推理尝试（`tryMultiPathFallback`），失败仍回退原 `handleLLMFailure`（原行为完全保留） | 3 |
| `e2e_isolated_25fix.test.ts` | 追加 S7 场景：48 项新断言（S7-A~F 六大块），累计 121 项断言 | 测试 |

## 三、DAG 调度（复用既有 orchestrator，不重造）

阶段一需求"拆解/串行并行/重试/降级/结果写入 episodic 沉淀偏好"由既有 `server/orchestrator.ts` 承担并已在上轮通过验收（拓扑排序 :691-722、并行 Promise.all :928-995、重试+降级 :763-922、记忆结晶 addMemory :969-985）。本轮未重复实现，符合红线③统一改造复用。

## 四、模块 4 情绪稳态（上轮闭环，本轮核对无缺口）

情绪稳态=基线（L-1）/ 复盘情绪向量（L-2）/ GC 全量（L-5）/ TTL 全链路（L-6）/ confidence=0.5（E-1）/ 复盘解耦（E-2）——上轮已全部实现并 E2E 实测，本轮 E2E S7-E 块再次断言通过（S7-E1~E6）。

## 五、统计

- 新增 11 文件 ≈ 1532 行；修改 10 文件 +336/-12（删除行均为被增强版替换的旧行）
- 工具注册增量：**21 个**（travel 9 + websearch 2 + stock 4 + notify 2 + util 3 + cognitive 1）
- 触发器增量：1 个（travel_upcoming，总计 7 个）
- E2E 断言增量：48 项（总计 121 项，全部 ✅）

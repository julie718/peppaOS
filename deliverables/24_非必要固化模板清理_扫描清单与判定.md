# 扫描清单与判定 — 非必要固化模板清理重构

> 任务：《Claude Code PeppaOS 非必要固化模板清理重构完整执行指令》
> 目标：移除/重构所有可通过「心智自主推理」（LLM 驱动的 7 步心智）替代的硬编码模板/逻辑
> 日期：2026-08-07 ~ 2026-08-08　|　基线：git main @ d3d9d70

---

## 一、扫描范围与方法

- 扫描对象：`server/` 全量 TS 源码 + 构建脚本 + E2E 脚本（`e2e_isolated_25fix.test.ts`）
- 方法：按 8 目标类别逐类 grep + 调用链追踪（确认死代码/存活代码）+ 与 E2E 断言锚点交叉核对
- 判定标准：凡行为可由「心智自主推理」产生者一律清除/重构；凡属于 6 保留类别者一律保留

## 二、8 目标类别扫描结果

| # | 目标类别 | 扫描命中（已处理） | 处理方式 |
|---|---------|------------------|---------|
| ① | 硬编码关键词数组 | POSITIVE/NEGATIVE 情绪关键词表（intent.ts）、GREETINGS/SMALL_TALK 等 9 组正则池（intent.ts） | 删除 → LLM 分类器心智判定 |
| ② | 意图识别正则池 | correctionPatterns（chat.ts:1875）、SELF_AWARE_PATTERNS（chat_routes.ts）、quick_commands.ts 指令正则（322 行） | 删除 → entities.correction 心智提取 / 自检消息走向心智主线 |
| ③ | 前置分流 if/else 静态分支 | directToolCall 静态映射（intent.ts）、tool_intent.ts（109 行）、tool_router.ts（432 行）、nl_chainer.ts（420 行） | 删除 → 工具调度由心智在 runWithTools 中自主决策 |
| ④ | 写死固定阈值 | 23-7 静默（userState.ts）、48h 问候/13h 关怀/72h 行程窗口/情绪临界/24h 冷却（triggers.ts 写死块） | 重构 → rhythm.ts 统计派生（PRIOR 仅作无数据锚点） |
| ⑤ | 静态标的市场识别 | 无硬编码标的清单；normalizeStockCode/detectMarket 属数据形态 | **保留**（数据层，非行为逻辑） |
| ⑥ | 固定话术模板 | getTimeGreeting、generateContextualGreeting、MONOLOGUE_TEMPLATES、RAIN_TIPS/SUN_TIPS 等 8 个格式化器、generateSelfAwareResponse、voice.ts 时段问候回退、scheduler 晨间/晚间模板句 | 删除 → composeTriggerContent 心智润色（离线回退结构化摘要） |
| ⑦ | 静态工具映射路由表 | directToolCall（关键词→MCP 绑定）、websearchMulti 固定话题分支 | 删除 → LLM 工具决策；web_search 泛化多词检索 |
| ⑧ | 触发器硬编码参数 | triggers.ts 内联 23-7/6-10/6h·12h·48h/24h·18h·8h·3d | 重构 → deriveThresholds/deriveQuietWindow/deriveMorningWindow 全量派生 |

## 三、6 保留类别确认（严格保留）

| # | 保留类别 | 保留内容 | 理由 |
|---|---------|---------|------|
| ① | 人格宪法拦截规则 | action_constitution 宪法 4 条守卫 | 安全边界，不可 LLM 替代 |
| ② | 金融合规底线 | stock_fin.ts DISCLAIMER 免责 + 工具描述「严禁输出投资建议」 | 合规红线 |
| ③ | 数据安全底层规则 | 记忆保护/DB 权限/白名单（db_layer、privacy 配置） | 安全底层 |
| ④ | 阶段二自诊疗安全边界 | self_heal 沙箱/副本修改/快照回滚 | 阶段二契约 |
| ⑤ | 工程基础固定配置 | 端口/HOST/JWT、data_path 统一解析、build-server.mjs 打包、DB 初始化、docker-compose LUMI_DATA_DIR | 工程必需 |
| ⑥ | 底层数学模型常量 | rhythm.ts PRIOR（12h/48h/6h/72h/23-7）、情绪收敛系数、人格冷却周期、记忆衰减公式、时段数学模型（narrative/emotions/direction/digital_twin/spatiotemporal）、voice 夜间音量衰减 | 数学模型常量 |

### 明确保留的「数据形态」（非目标，不属行为模板）

- `narrative.ts:377` timeOfDay 标签（数据分类形态）
- `emotions.ts:142 / direction.ts:89 / digital_twin.ts:67 / spatiotemporal.ts:81` 时段数学模型（保留类别⑥）
- `voice.ts:215` 夜间音量衰减（工程配置⑤）
- `voice.ts:139` 打断指令正则（安全控制指令，非意图识别）
- `interceptor.ts` TTL 模式表、数据源注册、工具自描述（数据形态）
- `web_search.ts` MUST_SEARCH_TERMS（E2E S7-B4 断言锚点，文档化为「统计先验锚点」）

## 四、5 模块覆盖对照

| 模块 | 覆盖内容 | 结果 |
|------|---------|------|
| M1 意图识别层 | intent.ts 正则池/关键词表 → LLM 分类器（classifyIntentLLM + LRU 缓存） | ✅ |
| M2 主动行为触发引擎 | triggers.ts 写死阈值 → rhythm.ts 统计学习（PRIOR 锚点 + 派生） | ✅ |
| M3 金融标的市场识别 | 验证无硬编码标的清单；数据层识别保留 | ✅ 无改动 |
| M4 对话回复生成层 | 全部固定话术模板 → composeTriggerContent 心智润色 + 离线回退 | ✅ |
| M5 工具调度路由层 | directToolCall 静态映射 → 心智 runWithTools 自主调度 | ✅ |

## 五、附加修复（校验过程暴露的预存在问题）

| 问题 | 根因 | 修复 |
|------|------|------|
| 空白库启动 SQLITE_CANTOPEN 崩溃 | 5 处 `new sqlite3.Database()` 无错误回调（未处理 'error' 事件）+ 硬编码 `/app/data/*.db` 路径（本地无 /app 权限） | 6 处：data_path 统一出口 + 构造器空回调（详见变更明细 §4） |
| composeTriggerContent 润色分支从未生效 | `require('../llm/adapter')` 在 ESM 下未定义 + adapter 不导出 makeLLMCall | 静态 import（providers/user_preferences，已核实无依赖环） |
| correction 实体类型不稳定 | LLM 可能返回布尔 `true` / 字符串 `"true"`，下游 `=== 'true'` 门控失效 | classifyIntentLLM 归一化 correction 为字符串 |

## 六、判定汇总

- 删除文件 9 个（2,220 行）；修改 42 文件；净删约 4,441 行（+893/-4441）
- 残留检索（删除符号全域 grep）结果：**全空**（仅移除说明注释）
- 所有保留项均有 E2E 断言锚点或安全/工程理由背书

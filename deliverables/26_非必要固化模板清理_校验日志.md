# 校验日志 — 非必要固化模板清理重构（7 步串行验证）

> 执行时间：2026-08-07 ~ 2026-08-08　|　验证方式：本机 macOS 隔离环境（Docker 行为经 LUMI_DATA_DIR=/app 对等验证）

---

## 步骤 1：残留检索（必须为空） ✅

对全部删除符号做全域 grep（`server/ --include="*.ts"`，排除 .test/.spec）：

| 删除符号 | 结果 |
|---------|------|
| getTimeGreeting / generateContextualGreeting / generateSelfAwareResponse / SELF_AWARE_PATTERNS / MONOLOGUE_TEMPLATES / RAIN_TIPS / SUN_TIPS / updateComprehension / generateClarification / correctionPatterns / GREETINGS / SMALL_TALK / COMMAND_PATTERNS | **全空**（仅移除说明注释，符合任务要求） |

**结论：残留模板 0 处，通过。**

## 步骤 2：TypeScript 编译（0 错误） ✅

```
npx tsc --noEmit   →   TSC_EXIT=0（两次运行：路径修复前 0、rhythm/intent 修改后复跑 0）
```

**结论：0 错误，通过。**

## 步骤 3：构建（exit 0） ✅

```
npm run build:server   →   [build-server] Generated dist-server/server.mjs + entry.cjs + hide-console.cjs
                           BUILD_EXIT=0
```

- build-server.mjs 已删除恒失败的 proactive tsc 编译块（esbuild 静态打包 proactive 已验证：bundle 内 L96496+ deriveRhythmProfile、L96619+ composeTriggerContent，无外部化 require）
- 修复后复跑 exit 0

**结论：构建干净通过。**

## 步骤 4：空白库启动（0 FATAL，health 200） ✅

首次运行复现 FATAL（`SQLITE_CANTOPEN` 崩溃），系统性定位（构造器打点追踪 → `scripts/tmp_boot_trace.mts` 定位 `life/index.ts:144`），修复 6 处后复跑：

```
LUMI_DATA_DIR=/tmp/mayos_verify_blank LIFE_DB_PATH=/tmp/mayos_verify_blank/life.db PORT=3999 HOST=127.0.0.1 JWT_SECRET=verify-secret npx tsx server.ts
→ grep -ci fatal 日志: 0
→ curl http://127.0.0.1:3999/health: {"status":"ok","timestamp":...}   (200)
→ [LifeDB] 已连接: /tmp/mayos_verify_blank/life.db
→ [LifeDB] 迁移完成: 14 张表, 0 个错误
→ /tmp/mayos_verify_blank/data/peppa.db 创建成功（495KB）
→ MCP filesystem: 14 tools discovered (total: 188)
→ 空白库无 interactions 表 → 交互计数查询优雅降级（warn，不再崩溃）
```

**结论：0 FATAL + health 200，通过。**

## 步骤 5：E2E 149/149（断言编号保留，验证目标更新为心智新行为） ✅

```
TZ=America/New_York npx tsx e2e_isolated_25fix.test.ts
→ 通过: 149  失败: 0  ✅ 全部 E2E 场景通过
```

- 断言总数保持 **149**（S1-S7 124 + S8 25），S8-L4a 满足 `assertionTotal === 73 && assertionPassed >= 70`
- `__forcedHour` 注入（12 日间 / 23 深夜）与 TZ 解耦
- 数据流事实核验：隔离库空 → readInteractionTimestamps 空 → gapFactor=1 → PRIOR 值不变 → S5-6..10 数字稳定
- **rhythm.ts/intent.ts 修改后复跑仍 149/149（零回归）**

## 步骤 6：四大人工场景复现（心智行为） ✅

独立验证脚本（`scripts/refactor_scenarios_verify.mts`，真实 LLM：deepseek-v4-flash + DEEPSEEK_API_KEY）：

| 场景 | 验证点 | 结果 |
|------|--------|------|
| 1 M1+M5 意图心智化 | 怀旧谈心→conversation+valence<0；时效规划→question/web；修正→correction='true'；情绪透传 | 5/5 ✅ |
| 2 M2 统计派生 | 问候阈值 clamp 派生（28.8h/96h）；冷却 A≠B；亲密放宽临界；日间/深夜画像静默窗口学习；晨间跟随；无数据回落先验 | 7/7 ✅ |
| 3 M3 金融合规 | DISCLAIMER 在位；4 工具描述禁令；normalizeStockCode 数据层映射；非 6 位透传 | 5/5 ✅ |
| 4 M4 生成润色 | 离线回退结构化摘要；无效 Getter 安全回退；4-80 字校验；场景档位模型；真实 LLM 润色自然语言 | 5/5 ✅ |

**结论：22/22 通过。** 过程中暴露并修复 2 个真 bug（详见变更明细 §5）：correction 归一化、润色分支 require→import。

## 步骤 7：git diff 审计 ✅

- 51 文件 +893/-4441：净删 3,500+ 行，与「清理固化模板」目标一致
- 抽查审计通过：intent.ts 正则池删除→LLM 分类器、triggers.ts 阈值派生、scheduler/voice/state/idle_brain 话术→composeTriggerContent、userState 23-7→rhythm 派生（含容灾）、web_search 泛化检索、build-server 清理、chat.ts correction 门控 + 重逢问候
- 保留项复查：PRIOR/MUST_SEARCH_TERMS/金融合规/self_heal 均在位
- 未跟踪文件确认：deliverables/（前会话交付物）、server/proactive/rhythm.ts（本重构新增）、server/self_heal/（阶段二遗留）、docker-compose.yml.bak/project_tree.txt（预存在）

## 附：预先存在单元测试失败（非本重构引入）

`npx vitest run`（test/ 目录）8 个失败项，与本次重构无关（重构前已失败，文件未被本重构触碰）：

| 测试文件 | 失败数 |
|---------|--------|
| adapter_registry_external_tools | 1 |
| emotional_state | 2 |
| language | 1 |
| lap_policy | 1 |
| legal_semiauto_workflows | 1 |
| rtf_extraction | 1 |
| wake_detector | 1 |

**判定：遗留问题（阶段一/二遗留，与本任务无因果关系），如实记录不隐瞒。**

## 结论

7 步串行验证全部通过，无失败阻断项。功能经 E2E 149/149 + 人工场景 22/22 双重验证。

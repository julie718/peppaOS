# 5 套标准 MCP 完整源码 + DAG/自主动机/认知推理/情绪记忆全部修改代码片段

> 交付物 ② —— 完整源码见对应文件（行号为 2026-08-08 版本），本文档收录核心片段与全部修改点，便于审计快速定位。

## 一、5 套标准 MCP（server/tools/mcp_servers/）

### 1. travel-cal-mcp（travel_cal.ts）—— 加密行程 + 提醒阈值 + 数据源对接 + 偏好过滤

**加密（AES-256-GCM，密钥 = sha256(`travel-cal:${JWT_SECRET||seed}`)，payload = base64(iv12+tag16+data)）：**
```typescript
import { createHash, randomBytes, createCipheriv, createDecipheriv } from 'crypto';
function deriveKey(): Buffer {
  const seed = process.env.JWT_SECRET || 'peppa-travel-dev-seed';
  return createHash('sha256').update(`travel-cal:${seed}`).digest();
}
export function encryptTravelPlan(plan: Record<string, any>): string {
  const iv = randomBytes(12);
  const cipher = createCipheriv('aes-256-gcm', KEY, iv);
  const enc = Buffer.concat([cipher.update(JSON.stringify(plan), 'utf8'), cipher.final()]);
  const tag = cipher.getAuthTag();
  return Buffer.concat([iv, tag, enc]).toString('base64');
}
```

**行程临近批量推送（行程触发器调用）：**
```typescript
export async function pushUpcomingTravelInfo(userId: string, withinHours = 72): Promise<number> {
  const upcoming = await getUpcomingTravels(userId, withinHours);
  let pushed = 0;
  for (const row of upcoming) {
    const plan = decryptTravelPlan(row.encrypted);
    const wx = await travelWeather({ city: row.destination }).catch(() => '天气暂不可用');
    const content = `✈️ 行程临近：${row.title} → ${row.destination}${row.depart_at ? '，出发 ' + row.depart_at : ''}\n${wx}`;
    try { pushNotification(userId, { type: 'travel', title: `行程提醒：${row.destination}`, message: content }); } catch {}
    pushed++;
  }
  return pushed;
}
```

**票务诚实降级（不虚构数据）：**
```typescript
async function flightAdapter(from: string, to: string, date: string): Promise<TravelAdapterResult> {
  const amapKey = process.env.AMAP_KEY;
  if (!amapKey) return { available: false, reason: '未配置机票数据源（AMAP_KEY），无法查询实时航班', data: { suggestion: `可在出行前 48h 内在购票平台确认 ${from} → ${to} 航班与票价` } };
  // 配置密钥后走高德真实接口（预留真实对接）
}
```

### 2. web-search-mcp（web_search.ts）—— 多源检索 + 时效 + 去偏见

**24h/7d 时效过滤（纯函数）：**
```typescript
export function isWithinWindow(pubDate: string | undefined, hours: number): boolean {
  if (!pubDate) return true;
  const t = Date.parse(pubDate);
  if (isNaN(t)) return true;
  return Date.now() - t <= hours * 60 * 60 * 1000 && t <= Date.now() + 60 * 60 * 1000;
}
```

**多源并发抓取 + 标题去重 + 多源标注：**
```typescript
export async function fetchMultiSource(keyword: string, hours: number, limit: number): Promise<Array<FeedItem & { sources: string[] }>> {
  const sources = NEWS_SOURCES.slice(0, 8);
  const raw = await Promise.all(sources.map(async s => {
    const items = await fetchRSS(s.url);
    return items.map(i => ({ ...i, source: s.name, category: s.category }));
  }));
  // 关键词过滤 → 时效过滤 → 标题归一化去重（同题多源合并标注 sources）
}

export const MUST_SEARCH_TERMS = ['时事', '国际', '战争', '冲突', '选举', '峰会', '关税', '制裁', '美联储', '央行', '最新消息', '突发'];
```

**多源对比去偏见（不替用户下结论）：**
```typescript
lines.push('⚖️ 去偏见提示：以上为各源原始报道并列，未做立场过滤；如各源表述冲突，请以原始出处核实。');
```

### 3. stock-fin-mcp（stock_fin.ts）—— 客观陈列 + 严禁投资建议

**免责声明（所有工具描述与输出强制携带）：**
```typescript
const DISCLAIMER = '\n⚠️ 以上仅为客观数据陈列，不构成任何投资建议。投资有风险，决策需自行判断。';
```
腾讯行情解析纯函数（离线可测）、K 线（ifzq.gtimg.cn）、公告（np-anotice）、板块（push2.clist）均免费接口，失败自动降级返回提示。

### 4. notify-mcp（notify.ts）—— 三通道推送

```typescript
export function notify(userId: string, payload: NotifyPayload): boolean {
  try { pushNotification(userId, { type: payload.type, title: payload.title, message: payload.message }); } catch {}
  try { emitProactivePush({ scene: payload.scene || payload.type, content: `${payload.title}\n${payload.message}` }); } catch {}
  logger.info(`[NotifyMCP] 推送 [${payload.priority || 'normal'}] ${payload.type}: ${payload.title} → ${userId}`);
  return true;
}
```

### 5. util-mcp（util.ts）—— 高精度计算 + 单位换算

**BigInt 定点 18 位高精度（递归下降解析，无 eval）：**
```typescript
export function calculate(expr: string): { ok: true; value: string } | { ok: false; error: string } {
  // tokens: (\d+(?:\.\d+)?(?:e[+-]?\d+)?|[+\-*/%^()])
  // parseExpr → parseTerm（* / % 整除保留18位）→ parseFactor（^ 整数指数 0-64）→ parseUnary
}
```
实测：`0.1+0.2 = 0.3`（无浮点误差）、`1/3 = 0.333333333333333333`、除零返回错误文案不抛异常。

**单位换算**：长度/重量/温度（非线性单独处理）/时间/面积/体积/数据量精确系数表 + frankfurter 免费汇率（失败降级静态表）。

### 共享工厂（mcp_helpers.ts）—— 5 套零重复编码

```typescript
export function buildMcpServerFromRegistry(name: string, version: string, registry: ToolRegistry, toolNames: string[]): McpServer {
  const mcp = new McpServer({ name, version }, { capabilities: { tools: {} } });
  // zod inputSchema（paramsToShape 由 JSON Schema 转换）+ 同一 handler 直调 ToolRegistry
}
```

## 二、DAG 调度（复用既有 orchestrator，代码片段见上轮交付）

需求"拆解/串行并行/重试/降级/结果写入 episodic 沉淀偏好"：`server/orchestrator.ts` 拓扑排序 :691-722、并行 Promise.all :928-995、重试+降级 :763-922、addMemory :969-985 —— 上轮已验收，本轮零改动（红线③）。

## 三、自主动机（模块 2）全部修改代码片段

### 数字孪生（digital_twin.ts）
```typescript
export async function collectBehavior(userId: string, dimension: TwinDimension, tag: string, amount = 0.1): Promise<void> {
  if (!TWIN_DIMENSIONS.includes(dimension)) return;
  await bumpPreferenceTag(userId, `孪生-${dimension}-${tag}`, amount); // 复用 user_preference_tags 表，不新建存储
}
export function predictBehaviorFromTags(tags, hour): BehaviorPrediction[] { /* 纯函数：按维度聚合 → 置信度 = min(0.9, 0.35+Σw*0.5) → 预判文案 */ }
```

### PSI 动机引擎（psi_motivation.ts）
```typescript
export function tensionForNeed(need: PsiNeed, minutesSinceLast: number, travelSoonHours?: number): number {
  if (need === 'planning') return travelSoonHours === undefined ? 0.15 : Math.min(1, 0.2 + (72 - travelSoonHours) / 72 * 0.8);
  const T50 = need === 'curiosity' ? 6 * 60 : 12 * 60;
  return Math.min(1, minutesSinceLast / T50 * 0.7);
}
export function pushAllowed(activeDays: number, lastPushAt: number, now = Date.now()) {
  const frequency = activeDays >= 7 ? 24 * 60 * 60 * 1000 : 3 * 24 * 60 * 60 * 1000; // 老用户 1 天 / 新用户 3 天
  return { allowed: now - lastPushAt >= frequency, frequency: frequency / (24 * 60 * 60 * 1000) };
}
export async function generateIdleBriefing(userId: string): Promise<string | null> {
  const items = await fetchMultiSource('', 72, 6); // 复用多源底座
  notify(userId, { type: 'news', title: '📰 资讯简报', message: briefing, scene: 'psi_briefing', priority: 'low' });
}
```

### 行程临近触发器（triggers.ts 追加）
```typescript
export const travelUpcomingTrigger: ProactiveTrigger = {
  name: 'travel_upcoming',
  check: async (): Promise<TriggerResult> => {
    const userId = getActiveUserId();
    if (!userId || userId === 'anonymous') return { triggered: false };
    if (isLateNight()) return { triggered: false };
    if (!cooldownOk('travel_upcoming', 8 * 60 * 60 * 1000)) return { triggered: false };
    const pushed = await pushUpcomingTravelInfo(userId, 72);
    if (pushed <= 0) return { triggered: false };
    return { triggered: true, scene: 'travel_upcoming', reason: `${pushed} 个行程 72h 内临近`, content: `行程临近提醒：${pushed} 个行程即将出发，详情与目的地天气已推送。` };
  }
};
```

### IdleBrain 长待机资讯简报接线（idle_brain.ts 新增步骤 5）
```typescript
// 阶段一·模块2: 5. 空闲资讯简报
try {
  const { generateIdleBriefing } = await import('./psi_motivation');
  const briefing = await generateIdleBriefing(targetUserId);
  if (briefing) logger.info(`[IdleBrain] 空闲资讯简报已生成 → ${targetUserId}`);
} catch (e: any) { logger.warn(`[IdleBrain] 资讯简报跳过: ${e?.message || '网络不可用'}`); }
```

## 四、认知推理（模块 3）全部修改代码片段

### 多路径交叉校验（multi_path_reasoner.ts，纯函数）
```typescript
export function crossValidatePaths(paths: PathResult[], threshold = 0.5): CrossValidateOutput {
  // 结论关键词归一化分组 → agreement = 最大组/总数
  // 严格大于阈值才判定共识（2 条链路各执一词时 0.5 不算共识，必须过半）
  const agreed = agreement > threshold && groups[0][0].conclusion.length > 0;
  // 共识 → finalConfidence = min(0.95, avg × (0.6 + agreement×0.4))
  // 分歧 → finalConfidence = 0.3（压低置信，提醒外部核实）
  // 分歧时 conflictingPoints 收集各链路疑点
}
export const REASON_PERSPECTIVES = [理性实证, 反面证伪, 多因合流];
export async function multiPathReason(question, opts) { /* Promise.all 三链路并行 + 45s 超时 + LLM 可注入 */ }
export async function tryMultiPathFallback(question: string): Promise<string | null> {
  // 命中 /为什么|如果|会怎样|原因|影响|预测|推测|导致|假设/ 才尝试；无模型返回 null 由调用方回退
}
```

### chat.ts LLM 全失败兜底接线（保留原 handleLLMFailure 行为）
```typescript
} catch (fallbackErr: any) {
  // Both primary and fallback LLMs failed — use cognitive fallback
  let cfText = '';
  try {
    const { tryMultiPathFallback } = await import('../cognition/multi_path_reasoner');
    cfText = (await tryMultiPathFallback(text)) || '';
  } catch {}
  if (cfText) responseText = cfText;
  else { const cf = handleLLMFailure(cognition.intent, fallbackErr); responseText = cf.responseText; }
}
```

### 资讯阅读后人格微调（personality.ts + web_search.ts 接线）
```typescript
// personality.ts: adaptToEvent 事件类型新增
case 'news_reading':
  delta[6] = 0.003; // 好奇心+
  delta[0] = 0.003; // 开放性+
  break;
// web_search.ts: websearchMulti handler 内（阅读行为打点 + 微调）
await collectBehavior(userId, '阅读', keyword.slice(0, 10), 0.1).catch(() => {});
await getPersonalityEngine().adaptToEvent({ type: 'news_reading' }).catch(() => {});
```

## 五、情绪记忆（模块 4）—— 上轮闭环零改动

情绪稳态=基线（L-1）/ 复盘情绪向量（L-2）/ GC 全量（L-5）/ TTL 全链路（L-6）/ confidence=0.5（E-1）/ 复盘解耦（E-2）——上轮已交付，本轮 E2E S7-E 块 6 项断言复核通过，无代码修改。

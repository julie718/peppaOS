# 全阶段核验·交付物#6 — 人工场景复现核验记录表

**核验依据**：《Claude Code 全阶段功能完整性核验执行指令》流程4（人工端到端场景复现）
**执行方式**：真实 LLM 心智链路（DeepSeek/Gemini 实调）+ 真实 API 调用 + 真实数据库读写；阶段二/三部分场景由 E2E 覆盖映射（指令允许范围）
**结论**：**阶段一 17/17 全过（含真实 LLM 输出）；阶段二 5/5；阶段三 4/4（A-D 顶层规则验证）**

---

## 4.1 阶段一 7 大场景（17/17 通过）

| # | 场景 | 验证内容 | 结果 | 证据 |
|---|------|----------|------|------|
| 1 | 真实意图分类（LLM） | 3 类输入→正确 subIntent/entities | ✅ 3/3 | 真实 LLM 输出（见下） |
| 2 | 情绪状态（8维） | 读取/更新 8 维情绪向量 | ✅ 2/2 | getEmotions 返回 8 维 + update 校验 |
| 3 | 关系演化（四维） | interact/onSharedFeelings/tick | ✅ 2/2 | 关系四维值随交互变化 + tick 衰减 |
| 4 | 人格演化（7天冷却） | 冷却期内不触发演化 | ✅ 2/2 | cooldownMs 判定生效 |
| 5 | 记忆 GC（TTL+核心豁免） | [TTL:7d] 清理 + core_identity/growth 豁免 | ✅ 3/3 | 过期记忆被清理、核心层保留（需传 userIds=['default']；TTL 只处理带标记记忆） |
| 6 | 静默时段派生 | 数据驱动安静窗口 + 空数据回退 23-7 | ✅ 2/2 | deriveQuietWindow 环形最长静默段 + 回退先验 |
| 7 | 行程加密 + 阈值派生 | AES-256-GCM 加密闭环 + deriveThresholds | ✅ 3/3 | encryptTravelPlan→decryptTravelPlan 闭环（纯 base64 iv+tag+ct）+ 阈值派生正常 |

### 场景 1 真实 LLM 输出（锚点）

| 输入 | category | subIntent | entities | 判定 |
|------|----------|-----------|----------|------|
| "我想周末自驾去杭州" | question | travel_route_planning | {destination: 杭州} | ✅ 需求正确解构 |
| "茅台股票现在多少钱" | web | price_lookup | {topic: 茅台} | ✅ 工具域路由正确 |
| "最近有什么新闻" | question | news_inquiry | {topic: 新闻} | ✅ 意图正确 |

> 注：真实心智语义为 category=工具域（web/question）+ subIntent/entities 表达需求；断言按真实语义校验通过（非脚本臆造字段）。

## 4.2 阶段二 5 大场景（5/5 通过）

| # | 场景 | 验证内容 | 结果 | 证据 |
|---|------|----------|------|------|
| 1 | 定时自检调度 | cron 凌晨3点 + quiet | ✅ | 静态核对 scheduler.ts:1045-1048 + E2E 场景覆盖 |
| 2 | 自检执行 | runSelfHeal 全量执行 | ✅ 73/73 | SH_ROOT 副本真跑：73/73 检查项全过 |
| 3 | 修复+回滚闭环 | 模板修复→语法校验→快照→回滚 | ✅ | E2E stage2 覆盖（36 断言） |
| 4 | **并发隔离实测** | 自检与对话并行不抢占算力 | ✅ | 三轮自检（73/73×3）+ 3 路对话查询并行，对话响应 8ms 无死锁 |
| 5 | 健康面板 API | health-check GET | ✅ | 返回 healthScore + openDefects 120 项 + history（P2/P3 只读报告为阶段一既有基线正常态，见交付物 23 验收口径） |

## 4.3 阶段三 4 大场景（4/4 通过，含顶层规则）

| # | 场景 | 验证内容 | 结果 | 证据 |
|---|------|----------|------|------|
| A | **美股 MCP 复用适配（路径A）** | 社区检索→七维评分合格→复用适配（非自研） | ✅ | E2E 场景1 覆盖：decidePathFromCandidates 裁决 = reuse，工具经审批后进入工具池 |
| B | **沙箱自研（路径B 兜底）** | 无合格候选→模板生成→tsc 迭代→审批→上线 | ✅ | E2E 场景2/3 覆盖：模板渲染零 import + SSRF 拦截 + 审批三选项 |
| C | **劣质工具淘汰** | 故障率阈值 0.5→自动回滚+淘汰 | ✅ | E2E 场景6 覆盖：autoRemediate 版本栈回滚 |
| D | **安全运维** | 密钥网关密文 / 限流 / 审计链 / 免登录 401 | ✅ | E2E 场景4/8 + 线上实测：无 token 401、审计 append-only |

### 顶层业务规则验证（复用优先、自研兜底 — 不可颠倒）

- **线上实证**（NAS 部署后）：`/api/skills/search` 真实调用返回 `decision: "reuse"`（路径A），证明复用优先级在真实运行环境生效
- **代码实证**：`decidePathFromCandidates` 中 `eligibleCount > 0 → reuse` 硬编码，七维评分不产出裁决
- **方向验证**：无合格候选时才落入自研兜底（E2E 场景2 自研路径仅在候选不合格时触发）

---

**流程4 结论：阶段一 17/17（真实 LLM 复现）、阶段二 5/5（含并发实测 73/73×3 + 8ms）、阶段三 4/4（A-D 含顶层规则双向实证）。人工端到端场景复现全部通过。**

# P2 灰度开启真实环境审计报告

> 审计时间：2026-08-11 23:53 ~ 2026-08-12 00:20（北京时间） / 2026-08-11 15:53Z ~ 16:20Z
> 环境：NAS 61.137.130.127，容器 peppaos（healthy），代码提交 18ae5a9（P2-migrate-on）
> 约束执行：允许真实对话产生真实业务数据；全程未修改任何代码。

## 1. 配置校验

| 项目 | 值 | 结果 |
|---|---|---|
| p2MigrateEnable | `true`（src/config/mindSwitch.ts:33，灰度开启） | ✅ |
| Phase3-B 总闸 sessionInnerTickOverride | `true` | ✅ |
| Phase3-B 白名单 overrideSessionWhitelist | `['conv_45e5748b-6ed2-4c35-b789-bb2156362f2e']` | ✅ 完整保留 |
| peppaos 容器 | Up（healthy），StartedAt 2026-08-11T15:53:21Z | ✅ |

## 2. 启动日志 P2 守卫输出

存在 ✅（container 启动时输出，逐字匹配）：
```
[P2-MIGRATE] paradigmGuard P2迁移守卫: p2MigrateEnable=true | 开启 — 旧life TICK 写核心心智状态被拦截，仅 InnerTick 可落库
```
（语义与预期一致：TICK 降级为只读快照观测模式，心智变更仅允许来自 innerTick）

## 3. tsc 编译

`npx tsc --noEmit` EXIT=0 ✅

## 4. 功能用例

### 用例1：S_A 白名单会话发送消息 —— 正常 ✅

- 会话 `conv_45e5748b-6ed2-4c35-b789-bb2156362f2e`（userId=fpj65njhjn）经 WebSocket `agent:chat` 发送消息，11s 收到回复。
- `[Phase3-MindProvider] session=conv_45e5748b-... mode=inner_tick_active snapshotId=16 mood=宽慰(0.65)` ✅ B 模式激活
- `[Phase2-InnerTick] session=conv_45e5748b-... chat轮次结束，调度异步心智回合` → `[InnerTick] 心智回合开始` ✅
- `[Phase2-InnerTick] ... turn=12 ok snapshot=#24 mood=温暖(0.70)` — inner_tick_snapshot#24（session_id=conv_45e5748b-...，trigger_source=chat_turn），快照内含 emotionDrift / desireEvolve / personalityDrift / relationshipAdjustment 四类完整字段 ✅
- P2-MIGRATE 落库日志 4 类齐全：
  - `[P2-MIGRATE] emotion_drift 落库 emotions#353: 温暖(0.70)`
  - `[P2-MIGRATE] desire_evolve 状态变更 desires#1/#5/#3 → abandoned` + `新增欲望 desires#10~13`
  - `[P2-MIGRATE] personality_drift 落库 personality#1: delta=[...]`
  - `[P2-MIGRATE] relationship_adjustment 落库 relationship_state: [0.72,0.70,0.68,0.53]`
- 数据库实测变更（数据来源均为 innerTick 推演）✅：
  - emotions: 352 → 353（温暖 0.70）
  - desires: 9 → 13（新增 #10-13，归档 #1/#3/#5）
  - personality: updated_at 更新（drift delta 应用）
  - relationship_state: [1,1,0.39,0.3] → [0.72,0.70,0.68,0.53]

> ⚠️ 观察项（不影响结论）：本轮 innerTick 首次 LLM 调用返回 content 为空（deepseek-v4-flash 间歇性只出 reasoning 不出 content），自动重试后成功，全程 5 分 45 秒。用例3 首次调用即成功，判定为间歇性延迟而非系统性故障；单层重试兜底有效。

### 用例2：10 分钟 life TICK 循环 —— 正常 ✅

- TICK 周期确认：15:53:40（启动轮）、16:03:40、16:13:40，16 步任务全部执行。
- 16:13:40 轮次完整观测：
  - `[P2-MIGRATE] P2 迁移开启：本 TICK 降级为只读快照观测模式（读取/计算/日志保留，写库被 paradigmGuard 拦截，仅 InnerTick 可变更心智状态）` ✅
  - `[ParadigmGuard] guardP2MentalStateWrite [P2-MIGRATE] 拦截旧路径直接写入核心心智状态表「emotion_state」...`（含完整调用栈）✅
  - `lifeDb.saveEmotionVector / decayDesires 写入被 P2 迁移守卫拦截，跳过落库` ✅
  - TICK 完成事件 `life_system_tick {"ok":true,"elapsed":2404,"errors":0}`（system_events#6347，16 步零错误）✅
- TICK 期间 4 表零写入实测：TICK 前后 counts 不变（emotions=353/desires=13/personality=1/relationship_state=1），4 表最后写入时间均为 16:06:05~06（innerTick 推演产物），TICK 未产生任何新增写入 ✅

### 用例3：普通非白名单会话 —— 正常 ✅

- 匿名会话（uid=anonymous）发送消息，会话 conv_b894e6f2-d43c-4159-bfc2-65c8eab34e3b。
- `[Phase3-MindProvider] session=conv_b894e6f2-... mode=old_life snapshotId=null` ✅
- innerTick 快照正常生成：`[Phase2-InnerTick] session=conv_b894e6f2-... turn=1 ok snapshot=#25` ✅
- 业务心智状态变更仅来自 innerTick 推演：emotion_drift emotions#354、desire_evolve desires#10/#11、personality_drift、relationship_adjustment [0.66,0.55,0.60,0.43]，数据库实测确认全部落库 ✅
- 旧 TICK 写库在普通会话同样被拦截（守卫为全局生效）✅

### 用例4：降级回滚验证 —— 未执行（说明）

审计约束第 1 条为「禁止修改代码」，回滚验证需修改 src/config/mindSwitch.ts（p2MigrateEnable=true→false）并重启容器，与约束冲突。该用例标注「可做，可选」，按约束跳过；回滚路径已由代码内注释与提交 b3aea38 声明支持（一键改开关重启即可）。

## 5. 数据库观测汇总

| 观测项 | 结果 |
|---|---|
| TICK 执行周期，旧 TICK 是否不再写入 4 张心智表 | 是 ✅ |
| innerTick 推演事件是否可以正常写入 4 张心智表 | 是 ✅ |
| inner_tick_snapshot 观测表落库（chat_turn 触发，含 session 归属） | 是 ✅（#24 S_A / #25 普通会话） |

## 6. 异常告警统计

- 本容器生命周期内 P2-MIGRATE 相关拦截日志共 27 条，全部为预期的旧路径写库拦截（emotion_state / desires / personality / relationship_state），**无非预期 P2-MIGRATE 拦截告警** ✅
- 非 P2 相关告警（记录备查，不影响本审计结论）：
  - `[InnerTick-RETRY] 首次心智推演失败（content 为空）` — 1 次，重试成功（见用例1 观察项）
  - `[MainLoop] high CPU load (3.2/4.4/2.4)` — 宿主负载波动，MainLoop 自动拉长间隔，与 P2 无关
  - MCP 部分本地 skill 连接失败（esbuild TransformError 等）— 存量问题，与 P2 无关

## 7. 最终工程结论

✅ **P2 灰度真实环境验证全部通过，已达成：旧硬编码 TICK 不再修改心智，全部心智演化由 LLM InnerTick 闭环产出，完成该阶段数字生命体范式对齐。**

- 旧 TICK：只读快照观测模式（16 步完整执行、写库全被守卫拦截、4 表零新增）
- 新心智源：runInnerTick → MentalEventItem → guardP2MentalStateWrite 守卫校验 → 4 表落库，S_A 会话与普通会话均验证通过
- B 模式（Phase3）与 P2 迁移（Phase4）协同正常：白名单会话 inner_tick_active 心智源注入 + 全局 LLM 推演落库

观察项（不阻断灰度，建议后续优化）：innerTick 推演在 deepseek-v4-flash 偶发「只出 reasoning 不出 content」时重试耗时可达 5+ 分钟；建议为 runInnerTick 的 makeLLMCall 增加超时/信号注入，并对空 content 情形考虑直接用 reasoning 内容兜底或换用 deepseek-chat。

【P2-GRAY-AUDIT-DONE】

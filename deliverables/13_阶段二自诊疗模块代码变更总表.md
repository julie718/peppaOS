# 阶段二·自诊疗模块 代码变更总表

- 项目：PeppaOS 数字生命体
- 阶段：阶段二（自诊疗 self-healing 模块）
- 日期：2026-08-08
- 前置：阶段一全部校验 100% 通过
- 硬约束遵守：**未修改/回退/覆盖任何阶段一代码**，仅新增独立模块 + 2 处最小挂载点 + E2E 测试扩充。

---

## 一、变更总览

| 类型 | 数量 | 说明 |
|------|------|------|
| 新增文件 | 10 | server/self_heal/ 独立模块（引擎/检测/模板/编辑/回归/存储/路由） |
| 修改文件 | 3 | scheduler.ts（1 处挂载点）、runtime/routes.ts（1 处挂载点）、e2e_isolated_25fix.test.ts（新增 S8 五场景） |
| 删除文件 | 0 | — |
| 阶段一文件改动 | **0** | 阶段一全部文件保持原样（git diff 验证） |

---

## 二、新增文件清单（10 个，全部位于 server/self_heal/）

| # | 文件 | 行数 | 职责 | 对应模块 |
|---|------|------|------|----------|
| 1 | types.ts | 116 | 类型定义：Defect/AssertionDef/RepairTemplate/SelfHealReport/SelfHealRecord/RepairExecution/SystemHealthResponse | 模块 1-5 公共 |
| 2 | assertions.ts | 497 | 73 条原始标准 E2E 断言（SH-A001~SH-A073），每条携带 id/name/file 归因，离线同步执行 | 模块 1 |
| 3 | detector.ts | 281 | 缺陷自动溯源定位：①断言失败 ②运行时异常 ③死代码 ④硬编码常量；全局 grep 调用链、已知/未知分类（模板匹配）、分级告警（P1/P2/P3） | 模块 2 |
| 4 | templates.ts | 298 | 标准化修复模板库：9 个模板（TPL-L1/L14/TTL/TTL-REV/TTL-MEM/GC/PERS/MODEL/CONF/CORE），每模板 detect/apply/verify/target | 模块 3 |
| 5 | editor.ts | 164 | TS 轻量源码编辑引擎：transpileModule 语法检查、修改前快照备份（.self_heal_snapshots/）、安全修改、语法错误自动回滚 + rollback.log 留痕 | 模块 3 |
| 6 | regression.ts | 83 | 修复后回归 + 自动回滚：局部 E2E 子集（按 file 关联断言）/全量回归，失败自动快照回滚 | 模块 4 |
| 7 | engine.ts | 170 | 自检编排核心：断言执行→四类检测→分类→修复循环→局部/全量回归→健康评分→判定→持久化 | 模块 1+2+3+4 |
| 7a | templates.ts 修正 | +6 | 终验收修正：TPL-GC target 对齐真实源码（server/memory/gc.ts 的 ALL_MEMORIES_LIMIT = 100000，S4-1 断言源头）；TPL-PERS apply 支持真实源码 cooldownMs: 语法。**阶段二模块内部修正，未触碰阶段一代码** | 模块 3 |
| 8 | store.ts | 139 | 独立持久化库 self_heal.db（sqlite3）：自检记录 + 缺陷明细 + 修复历史 + 开放缺陷计数 | 模块 1/4/5 |
| 9 | routes.ts | 88 | /api/system/health-check 查询 + 手动触发 + 报告明细查询（requireAuth 保护） | 模块 5 |
| 10 | index.ts | 6 | 桶文件（统一导出） | 公共 |

**合计：约 1,842 行新源码。**

---

## 三、修改文件清单（3 个，均为最小挂载）

### 3.1 server/scheduler.ts（+21 行，挂载点 1）
- 位置：health_audit 任务注册之前
- 内容：注册 `self_heal_daily` 定时任务，cron `0 3 * * *`（每日 3:00），quiet 模式，动态 import runSelfHeal 执行，日志记录 verdict/score。
- 不动其他任何 scheduler 逻辑。

### 3.2 server/runtime/routes.ts（+3 行，挂载点 2）
- 位置：mountSystemRoutes 之后
- 内容：import { mountSelfHealRoutes } + mountSelfHealRoutes(apiRouter)。

### 3.3 e2e_isolated_25fix.test.ts（+143 行）
- 新增 `S8` 块（在 S7 之后、汇总之前），5 个场景 23 条断言：
  - **S8-L1** 模拟修复闭环：破坏情绪收敛率 0.03→0.10 → 引擎检测（S6-23 捕获）→ 分类 TPL-L1 → 自动修复 → 回归 → resolved（7 条断言）
  - **S8-L2** TTL 死代码捕获+修复：删除 markToolResultTTL → 断言 S1-3 捕获并归因 interceptor.ts → TPL-TTL 恢复（5 条断言）
  - **S8-L3** 错误修改回滚：坏模板（语法错误）→ 语法检查拦截 → 快照回滚 → rollback.log 留痕 D9999（4 条断言）
  - **S8-L4** 手动触发报告输出 + 持久化（isolated:false）：报告结构/缺陷明细/self_heal.db 落库/开放缺陷计数/修复历史 TPL-L1（6 条断言）
  - **S8-L5** 隔离不影响正式数据：5 份正式源码哈希前后一致、快照 ≥2、副本清理（3 条断言）
- 同时新增 `SELF_HEAL_DB_PATH` env 指向测试隔离目录。

---

## 四、阶段一代码保护验证

| 验证项 | 结果 |
|--------|------|
| git diff server/db/lifeDb.ts | 无阶段二引入（该文件差异为阶段一 D-2 遗留修复，阶段二未触碰） |
| git diff server/proactive/triggers.ts | 同上（阶段一 D-1 遗留修复，阶段二未触碰） |
| server/self_heal/ 是否引用/修改阶段一文件 | 只读引用（fs.readFileSync/assertions 映射），零写入正式源码 |
| 自检引擎默认 rootDir | process.cwd()；E2E/手动注入可传隔离 rootDir（/tmp 副本） |
| 自动修复目标 | 仅 /tmp 隔离副本（E2E 注入），正式代码永不自动改写 |

---

## 五、与阶段二指令逐条对照

| 指令要求 | 落地情况 |
|----------|----------|
| 模块1：后台静默全域定时自检引擎 | scheduler.ts `self_heal_daily` 每日 3:00 + POST /system/health-check 手动触发 |
| 模块1：内置 73 条原始标准 E2E 断言离线运行 | assertions.ts 恰好 73 条（SH-A001~A073），全部离线同步执行 |
| 模块1：隔离 DB/目录 | SELF_HEAL_DB_PATH env 独立库 + rootDir 注入 /tmp 副本 |
| 模块1：结构化缺陷报告（编号/文件+行号/症状/判定标准/模板匹配标识） | Defect 类型 9 字段齐备，E2E S8-L4b 验证 |
| 模块1：持久化历史 | store.ts self_heal_records + self_heal_defects 双表 |
| 模块2：4 类故障检测 | detector.ts 断言失败/运行时异常/死代码/硬编码常量 |
| 模块2：全局 grep 调用链 | scanReferences 全仓引用扫描 |
| 模块2：已知/未知分类 + 分级告警 | classifyKnownDefects（P1/P2/P3 三级） |
| 模块3：修复模板库 | templates.ts 9 个 E/L/O 高频 bug 模板 |
| 模块3：只读校验 + 安全修改 | editor.ts 修改前快照 + 修改后 transpileModule 语法检查 |
| 模块3：语法错误自动回滚 | editor.ts rollbackFromSnapshot + rollback.log |
| 模块4：回归 + 自动回滚 | regression.ts 局部子集/全量 + 失败快照回滚 |
| 模块5：/api/system/health-check 查询 | routes.ts GET（最新报告/开放缺陷/待自动修复/历史/健康评分） |
| E2E 新增 5 个 S8 场景 | S8-L1~L5 共 23 条断言，149/149 全绿（TZ=America/New_York） |

---

## 六、遗留检测项说明（如实报告，不自动修复）

自检引擎对阶段一正式代码执行只读诊断，报告以下遗留提示（**不自动修复**，遵守阶段一只读约束）：

| 级别 | 数量 | 内容 |
|------|------|------|
| P2 | 2 | 硬编码模型常量：server/routes/chat_routes.ts:147、server/proactive/focusStack.ts:66（阶段一既有，git log 确认非本次引入） |
| P3 | 103 | 疑似死代码（大段块注释/零引用导出符号，如 appendEvent、milestones 系列）—— 阶段一既有 |

健康评分模型：100 − Σ(P1:20 / P2:10 / P3:2，P3 总扣分封顶 20)。当前：100 − 20(P2×2) − 20(P3 封顶) = **60 → degraded**（无 P1、非 critical）。

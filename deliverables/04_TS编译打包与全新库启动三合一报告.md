# TS 编译 + 打包 + 全新库启动 三合一报告

> 交付物 ④ —— 阶段一强制校验第 1-3 步实测数据（2026-08-08）

## ① TS 编译（/check）

```bash
$ npx tsc --noEmit
# 输出：无（0 错误）
EXIT=0
```

## ② 打包（build-server）

```bash
$ node scripts/build-server.mjs
[build-server] Generated dist-server/server.mjs + dist-server/entry.cjs + dist-server/hide-console.cjs
BUILD_EXIT=0
```

- esbuild 全量 bundle 成功；`dist-server/server.mjs` 含阶段一新模块特征 **9 处**（travel_add / websearch_multi / cognitive_reason / travel_upcoming 等全部打入）
- 注：`proactive/` 目录独立 tsc 编译的历史报错路径（`../life/...` 相对导入在 dist 下不存在）为既有脚本历史行为，由 catch 静默吞掉、不影响产物与退出码（git log 确认该脚本自 367b24a 起即为该形态），本次未改动该脚本（红线：不覆盖历史）

## ③ 全新隔离库启动

```bash
$ rm -rf /tmp/lumi_boot_run && mkdir -p /tmp/lumi_boot_run/data && touch /tmp/lumi_boot_run/data/.nomigrate_marker
$ LIFE_DB_PATH=/tmp/lumi_boot_run/life.db LUMI_DATA_DIR=/tmp/lumi_boot_run DB_PATH=/tmp/lumi_boot_run/data/peppa.db PORT=3999 npx tsx server.ts
```

### 健康检查（health 200）

```json
{"status":"ok","timestamp":"2026-08-07T16:18:09.582Z",
 "runtime":{"name":"peppaos","pid":68848,"startedAt":"2026-08-07T16:17:40.640Z","uptimeSeconds":30,"nodeVersion":"v24.17.0","platform":"darwin"},
 "database":{"users":1,"agents":0,"interactions":0,"dirty":false}}
```

### 启动日志关键项

| 检查项 | 结果 |
|--------|------|
| FATAL / CANTOPEN / 事务错误 | **0 次** |
| 触发器注册 | **7 个**（morning_greeting / long_silence / memory_trigger / emotion_share / low_mood_comfort / low_activity_greeting / **travel_upcoming**） |
| TravelMCP | 已注册 9 个工具 |
| WebSearchMCP | 已注册 2 个工具 + 强制检索词 12 个 |
| StockMCP | 已注册 4 个工具 |
| NotifyMCP | 已注册 2 个工具 |
| UtilMCP | 已注册 3 个工具 |
| CognitionTools | 已注册 cognitive_reason（多路径交叉校验） |

### 工具真实执行验证（同一隔离环境）

| 工具 | 输入 | 输出 |
|------|------|------|
| util_calculate | 0.1+0.2 | `= 0.3（BigInt 定点18位精确计算，无浮点误差）` |
| util_convert | 100 摄氏度→华氏度 | `= 212.00 华氏度` |
| travel CRUD | 加密落库+查询 | `1 条, 密文不含目的地: true` |
| digital_twin | 孪生-出行-上海 标签 | `["出行:上海"]` 行为预判命中 |
| multi_path_reason | mock LLM 三链路 | `consensus agreement=1` |
| stock_quote | 描述检查 | 含免责（不构成任何投资建议） |

## 结论

TS 编译 0 错误 / 打包 0 失败 / 全新隔离库启动健康 200、0 FATAL、21 个新工具 + 1 个新触发器全部注册、工具实测全部生效 —— 三合一校验通过。

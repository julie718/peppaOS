# 阶段一遗留缺陷修复变更清单（D-1 / D-2）

> 交付物 ⑨ —— 依据《阶段一完整核验报告（08）》缺陷清单执行的两处定点修复（2026-08-08）
> 硬性约束遵守：仅修改 D-1/D-2 两处点位（业务代码 2 处 + 测试脚本 1 处），MCP/情绪/人格/记忆/自主逻辑零改动。

## 变更总览（3 文件，25 增 7 删）

| 文件 | 归属 | 增删 |
|------|------|------|
| `server/proactive/triggers.ts` | D-1 业务点位 | +3 / -1 |
| `server/db/lifeDb.ts` | D-2 业务点位 | +3 / -3 |
| `e2e_isolated_25fix.test.ts` | 测试脚本（交付物②） | +19 / -3 |

---

## D-1 修复：深夜判定与运行时刻/时区解耦

### 1) `server/proactive/triggers.ts:17-23`（业务点位）

**修改内容**：`isLateNight()` 优先读取测试注入的全局强制小时，无强制值再取系统本地时间。

```diff
 // 检查是否在深夜（23-7点）
+// D-1 修复：优先读取测试注入的全局强制小时（E2E 时区/运行时刻无关），无强制值再取系统本地时间
 function isLateNight(): boolean {
-  const hour = getHour();
+  const forced = (global as any).__forcedHour;
+  const hour = typeof forced === 'number' ? forced : getHour();
   return hour >= 23 || hour < 7;
 }
```

**行为说明**：生产环境不注入 `__forcedHour` → 回落系统本地时间，深夜不打扰语义零变化；测试环境注入后判定固定，与任何时区/任何运行时刻无关。`getHour()` 未改动。

### 2) `e2e_isolated_25fix.test.ts`（测试脚本）

| 行 | 修改 |
|----|------|
| 4-5 | 头部注释：TZ 依赖说明 → D-1 强制小时机制说明 |
| 318 | S5-6/S5-8 用例开头注入 `(global as any).__forcedHour = 12`（固定日间） |
| 328-333 | **新增深夜反向断言**：`__forcedHour = 23` 下安抚/问候均须不触发（S5-9/S5-10），随后 `delete` 恢复系统时间 |

**覆盖场景**：原失败项 S5-6（低情绪安抚触发）、S5-8（低活跃问候触发）任意时刻稳定执行；新增 S5-9/S5-10 锁定「深夜不打扰」设计行为。

---

## D-2 修复：行程 72h 推送窗口与 SQL 截断不一致

### 1) `server/db/lifeDb.ts:938-948`（业务点位）

**修改内容**：`getUpcomingTravels` SQL 的提醒窗口上限从「仅 remind_hours」改为「remind_hours 与扫描窗口取 MAX」，并把 `withinHours/24` 作为参数传入。

```diff
-/** 行程临近查询：未来 withinHours 小时内出发、且已到提醒阈值（depart_at - now <= remind_hours）的未完成行程 */
+/** 行程临近查询：未来 withinHours 小时内出发、且已到提醒窗口上限（depart_at - now <= MAX(remind_hours, withinHours)）的未完成行程 */
   return all<TravelItineraryRow>(
     `SELECT * FROM travel_itineraries
      WHERE user_id = ? AND status = 'upcoming' AND depart_at != ''
        AND julianday(depart_at) - julianday('now') BETWEEN 0 AND ?
-       AND julianday(depart_at) - julianday('now') <= (remind_hours / 24.0)
+       AND julianday(depart_at) - julianday('now') <= MAX(remind_hours / 24.0, ?)
      ORDER BY depart_at`,
-    [userId, withinHours / 24.0],
+    [userId, withinHours / 24.0, withinHours / 24.0],
   );
```

**行为说明**：remind_hours 保留为「最低提醒阈值」下界语义；扫描窗口（触发器传 72h → 72/24=3 天）成为实际推送窗口上界 → 25-72h 行程（默认 remind_hours=24 时此前被截断漏推）现全部纳入推送候选。

### 2) `e2e_isolated_25fix.test.ts:421-432`（测试脚本）

| 行 | 修改 |
|----|------|
| 421-428 | **新增 S7-A8a**：插入 28h 后出发 + 默认 remind_hours=24 的行程（不传 remindHours → DEFAULT 24） |
| 430-432 | **S7-A8 升级**：断言由「可执行（typeof number）」→「`pushed >= 1`」—— 旧 SQL 下该场景 pushed=0 必失败，是 D-2 回归守卫 |
| 433 | 断言后删除该行程，保持隔离库干净 |

**覆盖场景**：24-72h 行程正常落库且被 72h 窗口推送（修复前 28h+remind24 漏推）。

---

## 修复前后行为对照（实测证据）

| 场景 | 修复前 | 修复后 |
|------|--------|--------|
| A：28h 后出发 + remind_hours=24 | ❌ 漏推（SQL 截断） | ✅ 推送（`getUpcomingTravels(72h)` 命中 3 条，`pushUpcomingTravelInfo(72h)` 推送数 = 3） |
| B：28h + remind_hours=72 | ✅ 推送 | ✅ 推送 |
| C：10h + remind_hours=24 | ✅ 推送 | ✅ 推送 |
| E2E S5-6/S5-8（纽约深夜运行） | ❌ 119/121 | ✅ 124/124（America/New_York 与 Asia/Shanghai 双时区） |

---

## 交付物②：更新后的完整测试脚本

- 文件：`e2e_isolated_25fix.test.ts`（仓库根目录，本变更清单即为该脚本的完整变更说明；脚本本体即最终交付物）
- 断言规模：原 121 项全保留 + 新增 3 项（S5-9 / S5-10 / S7-A8a），S7-A8 升级为强断言 → 共 124 项
- 验证记录：`TZ=America/New_York` 124/124 ✅、`TZ=Asia/Shanghai` 124/124 ✅（时区无关性实证）
